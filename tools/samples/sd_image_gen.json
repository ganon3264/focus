{
  "name": "sd_image_gen",
  "description": "Generate an image using a local SD.cpp instance.",
  "command": ["python3", "tools/samples/sd_image_gen.py"],
  "multimodal": true,
  "params": [
    {
      "name": "prompt",
      "type": "string",
      "description": "Text description of the image to generate. Tag order: [quality/meta/year/safety tags] [1girl/1boy/1other etc] [character] [series] [@artist] [general tags]",
      "required": true
    },
    {
      "name": "negative_prompt",
      "type": "string",
      "description": "Things to avoid in the generated image; default one provided if not specified.",
      "required": false
    },
    {
      "name": "width",
      "type": "integer",
      "description": "Image width in pixels. Default varies by model; safe range is typically 512-1536.",
      "required": false
    },
    {
      "name": "height",
      "type": "integer",
      "description": "Image height in pixels. Default varies by model; safe range is typically 512-1536.",
      "required": false
    },
    {
      "name": "loras",
      "type": "array",
      "description": "List of LoRAs to apply with their multipliers. Example: [{\"name\": \"lora_name_or_file\", \"multiplier\": 0.8}] or [\"lora_name_or_file\"]",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The exact name or filename stem of the LoRA as reported by sd_list_loras."
          },
          "multiplier": {
            "type": "number",
            "description": "Strength/scale of the LoRA adapter (default: 1.0)."
          }
        },
        "required": ["name"]
      },
      "required": false
    }
  ],
  "category": "Sample",
  "writes": false,
  "timeout": 300
}
